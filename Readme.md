# Boilerplate - Spring Boot

## 구성 (Services)
    - 기능(Feature)
        - 게시판 CRUD
    - 통합(Global)
        - log4js 설정
        - 어노테이션 추가
            - @LogExecutionTime: 메소드에 추가 시 로그에 해당 메소드 실행 시간 출력 (디버깅용)